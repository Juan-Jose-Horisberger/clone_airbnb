'use client'

interface Props {
    id: string
    label: string
    type?: string
    disabled?: boolean
    formatPrice: boolean
}

const Input = (props: Props) => {
    return (
        <div>Input</div>
    )
}

export default Input